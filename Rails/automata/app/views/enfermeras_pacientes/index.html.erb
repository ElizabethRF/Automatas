<p id="notice"><%= notice %></p>
<%= link_to 'Relacionar', new_enfermeras_paciente_path, class: 'btn btn-info'%>
<h1>Relación enfermeras pacientes</h1>
<b>Nota:</b>
<p>Una enfermera puede tener a varios pacientes asignados</p>
<p>Un paciente puede tener a varias enfermeras asignadas</p>
<table class="table table-striped">
  <thead>
    <tr>
      <th>Enfermera</th>
      <th>Paciente</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @enfermeras_pacientes.each do |enfermeras_paciente| %>
      <tr>
        <td><%= enfermeras_paciente.user.name + " " +enfermeras_paciente.user.lastname1 + " " +enfermeras_paciente.user.lastname2 %></td>
        <td><%= enfermeras_paciente.paciente.name + " " +enfermeras_paciente.paciente.lastname1 + " " +enfermeras_paciente.paciente.lastname2 %></td>
        <td><%= link_to 'Ver', enfermeras_paciente , class: 'btn btn-success btn-xs' %></td>
        <td><%= link_to 'Editar', edit_enfermeras_paciente_path(enfermeras_paciente), class: 'btn btn-warning btn-xs' %></td>
        <td><%= link_to 'Eliminar', enfermeras_paciente, method: :delete, data: { confirm: '¿Estás seguro? Esta acción no se puede deshacer' } , class: 'btn btn-danger btn-xs'%></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Relacionar', new_enfermeras_paciente_path, class: 'btn btn-info' %>
