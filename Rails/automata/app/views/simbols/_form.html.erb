<style>
.right{
  text-align: right;}
</style>
<%= render 'shared/errors', obj: @simbol %>

<div class="mx-auto">
    <div class="container">
      <%= form_for @simbol, :html => { multipart: true, :class => "form-horizontal image" } do |form| %>

      <div class="form-group row">
        <div class="control-label col-md-2 right">
        <%= form.label :Nombre %>
        </div>
      <div class="col-md-6">
          <%= form.text_field :name, class: "form-control" %>      </div>
      </div>
        <div class="form-group row">
          <div class=" col-md-2 right">
          <%= form.label :Símbolo %>
          </div>
      <div class="col-md-6">
          <%= form.file_field :picture, accept: 'image/jpeg,image/gif,image/png,image/jpg'%>
        </div>
        </div>

        <div class="form-group row">
          <div class=" col-md-2 right">
          <%= form.label :Detalles %>
          </div>
      <div class="col-md-6">
          <%= form.text_area :detail, id: :simbol_detail , class: "form-control"%>
        </div></div>

        <div class="actions">
          <div class="text-center col-md-12">
          <%= form.submit "Guardar" , class: 'btn btn-info'%>
        </div>
        </div>
      <% end %>
    </div>
  </div>  


<script type="text/javascript">
    $('#image_picture').bind('change', function() {
      var size_in_megabytes = this.files[0].size/1024/1024;
      if (size_in_megabytes > 5) {
      alert('Maximum file size is 5MB.');
    }
    });
</script>